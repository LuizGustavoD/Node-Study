## Aula 2 - Projeto Node

Aula para criação de projetos Node, utilizando o npm como gerenciador de pacotes para iniciar o projeto.

```bash
# Init a Node Project
npm init -y # Quick start
npm init # Custom config
```

### Extra:
Definição de qual tipo de módulo estamos utilizando no campo "TYPE", sendo commonjs ou module.
