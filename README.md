# Node Study

Repositório de estudos de Node.js e TypeScript, organizado por aulas.

## Sumário das aulas

1. **aula 1**: Introdução ao Node.js e execução de arquivo JavaScript fora do navegador (`node hello_world.js`).
2. **aula 2**: Inicialização de projetos Node com `npm init` e noção de tipo de módulo (`commonjs` vs `module`).
3. **aula 3**: Modularização em Node.js com comparação prática entre **CommonJS** (`require/module.exports`) e **ESM** (`import/export`).
4. **aula 4**: Continuação de módulos com foco em separação por camadas (ex.: `services`) e import/export via CommonJS.
5. **aula 5**: Uso de arquivos `.mjs`, exportações nomeadas/default e estratégias de importação de 1 ou múltiplas features.
6. **aula 6**: Gerenciamento de pacotes e dependências com NPM (`dependencies`, `devDependencies`, instalação, remoção e atualização).
7. **aula 7**: NPM Scripts, automações de ciclo de vida (`pre`/`post`) e padrões comuns como `dev`, `build` e `lint`.
8. **aula 8**: Variáveis de ambiente em Node.js com `.env`, execução com `--env-file` e separação de ambientes.
9. **aula 9**: Fundamentos de TypeScript como superset de JavaScript, diferenças entre erros de runtime e compile-time.
10. **aula 10**: Setup completo de TypeScript em projetos Node (`typescript`, `tsx`, `tsconfig`, transpile e scripts de desenvolvimento/build).
11. **aula 11**: Debugging em Node.js (breakpoints, call stack, inspeção de variáveis e integração com `launch.json` no VS Code).
12. **aula 12**: Diretório reservado para continuidade dos estudos (ainda sem conteúdo).

## Estrutura do repositório

Cada pasta `aula X` contém exemplos e anotações práticas daquele tema.
